# Minimal runtime deps. Embedding and reranking run directly on
# onnxruntime + tokenizers (see scripts/onnx_models.py) -- no
# sentence-transformers / torch needed. Fresh install shrinks from
# ~3 GB to ~200 MB.
# NOTE: keep this file ASCII-only; pip reads it with the locale
# encoding (e.g. cp950) on Windows and non-ASCII bytes break it.
chromadb==1.5.9
rank-bm25==0.2.2
onnxruntime==1.27.0
tokenizers==0.22.2
huggingface_hub==1.21.0
numpy==2.4.6
